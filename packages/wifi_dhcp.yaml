# substitutions:
#   friendly_name: "Boxe"
#   password: !secret password
#
#   wifi_ssid: !secret wifi_ssid
#   wifi_pswd: !secret wifi_password


wifi:
  networks:
  - ssid: ${wifi_ssid}
    password: ${wifi_pswd}

  ap:
    ssid: ${friendly_name}
    password: ${password}
