# substitutions:
#   friendly_name: "Boxe"
#   password: !secret password
#
#   wifi_ssid: !secret wifi_ssid
#   wifi_pswd: !secret wifi_password


wifi:
  min_auth_mode: WPA2
  networks:
  - ssid: ${wifi_ssid}
    password: ${wifi_pswd}


